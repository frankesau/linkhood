---
import LinkListItem from "./LinkListItem.astro";

const {linkListItems} = Astro.props
---

<ul role="list" class="list-none mt-0 p-0 flex flex-col gap-3">
    {
        linkListItems.map((linkListItem) => (<LinkListItem {...linkListItem}/>))
    }
</ul>
